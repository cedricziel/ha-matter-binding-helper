# Test dependencies for Matter Binding Helper integration tests
pytest>=7.0
pytest-asyncio>=1.4.0
websockets>=16.0
testcontainers>=4.14.2
requests>=2.28
docker>=7.0
pyyaml>=6.0  # fixtures read pinned Matter server image tags from docker-compose.yml
